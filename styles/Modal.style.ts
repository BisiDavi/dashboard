import { makeStyles } from "@material-ui/core";

export const modalStyles = makeStyles(() => ({
    modal: {
        alignItems: "center",
        justifyContent: "center",
        display: "flex",
    },
}));
